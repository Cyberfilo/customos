[project]
name = "customos"
version = "0.0.0"
description = "CustomOS workspace root — see README.md"
requires-python = ">=3.12"

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "customos-core",
    "profile-extractor",
    "customization-system",
]
